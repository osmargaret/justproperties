ADMIN[
    Done {
        Users => [
            [Table columns should be: 
                Details (Avatar,Name,Email,Phone), 
                Country,
                Properties (count),
                Active Subscriptions (e.g 2 Subscriptions | 20 Seats),
                Actions (View )
            ],
            [Add Search, Sort (by Name, Properties, Seats) ],
            [
                User details page should show details of user, 
                Buttons: Suspend, Delete, View Properties, View Subscriptions
            ]
        ],
        Subscriptions => [
            This should show a table with columns => [
                Date Purchased, Subscription_ID, Plan, User , Seats (2 of 5), Status, Expiry Date, View Invoice
            ],
            [View Invoice button should open modal: Show payment information]

            [Add Search by user, Filter by Date Range, Filter by Status, Sort (by Purchase Date,Expiry Date) ],
        ]
    }


    Promotions => [
        This should show a table with columns => [
            Date Purchased, 
            User,
            Type => [Blog Post, Featured (views & clicks) , Newsletters], 
            Property (hyperlink to property view), 
            Status, 
            View Analytics
        ],
        [View Analytics button should open modal: Show post views, property views/clicks, emails sent, emails opened, audience type]
        [Add Search by user, Filter by Date Range, Filter by Type, Filter by Status, Sort (by Date, ) ],
    ],
    Properties => [
        This should show a table with columns => [
            Date Listed, 
            User,
            Category & Type, 
            Property (hyperlink to property details), 
            Status, 
            Approve | Disapprove | Delete
        ],
        [Add Search by user & property tile, Filter by Category, Filter by Status, Sort (by Date, ) ],
        [
            Property Details page should contain => [
                Property details as entered by seller,
                Section to view subscription plan
                Section to view promotions & analytics
                Buttons to approve/disapprove/delete 
                Section to view moderation History
            ]
        ]
    ]
   
    Blog => [
        This should show a table with columns => [
            Date Created, 
            Owner,
            Title,
            Category & Tag
            Related Property, 
            Status
            View
        ],
        [Add Search by owner & tile, Filter by Category, Filter by Status, Sort (by Date, ) ],
        [
            Blog Post view page should contain => [
                Title, Content, Featured Image, Tag
                Section to view Promotion record (if exist)
                Section to view analytics,
                Hyperlink to Related Property ,
                Section to view Author,
                Button to edit|delete|regenerate (if AI-content)|
            ],
            Add Blog Create & Edit Pages
        ]
    ],
    Payments => [
        This should show a table with columns => [
            Date, Reference, Details, User , Amount, Status, View More
        ],
        [View more button should open modal: Show payment information]

        [Add Search by user / reference, Filter by Date Range, Filter by Status, Sort (by Date) ],
    ],
    Coupons => [
        This should show a table with columns => [
            Code, Details, Total Count, Qty Used, Status, Edit/Delete
        ],
        [Add Search by code, Filter by Status, Sort (by Qty Remaining, by Qty Used) ],
        Add Create & Edit Pages
    ],

    Profile - add notification settings, 
    Chat interface with whatsapp
    Notifications via email: [
        moderations-> notifications,
        blog comment -> notifications,
        blog post -> notifications

    ]
    change notifications page to moderations
    Settings => [
        [Settings->Content Settings : [
                Content Generation: Manual / AI ,
                Manual Timeframe hours,
                Content min & max word count, 
            ]
        ]
        [
            Settings->Countries page should be CRUD,
            [
                Each country should have config button which will open Country Config page which
                will consist of 'subscription plan prices','promotion plan prices','payment gateways',
                verification requirements, 
            ]
        ],
    ],

    in addition to this, I will like us to add verification fields to users table like address, govt_id_number, govt_id_expiry. Then we request users to upload documents like govt_id + address_proof which will be saved to media table and a moderation record will be created. 
    This verification could be done in the pro
],

Seller[
    List-Property =>[
        [remove promotion from this page. A payment cannot be related to both subscription and promotion at the same time],
    ],
    Listed Properties => [
        Add property first image thumbnail to table,
        Visibility of property depends on status and moderation relationship,
        Add Edit-Property page,
    ],
    Listed-Property Details view => [
        Show subscription used for property. Allow user to change subscription from subscriptions with available seats. 
        Add button to promote property- onclick show modal with promotional plans, coupon field and button to pay
        If promotion content exist, show promotion details and anaylytics.  => [
            Blog Promotion => [
                Show title of Post with href link,
                Show views, Date created, published,
            ],
            Newsletter => [
                Show email title and content,
                Show number of recipient, emails sent, emails opened,
            ],
            Featured Listing => [
                Show start date, target type, target count, 
                Show target_achieved
            ]

        ],
        if promotion exist but not content => [
            Blog Promotion => [
                if not available,show pending.. generating,
                if available, show button to preview => [
                    Show 3 posts and ask the user to pick 1. Each post shows title, content,
                ]
            ],
            Newsletter => [
                if not available,show pending.. generating,
                if available, show button to preview => [
                    Show 3 emails and ask the user to pick 1. Each email shows subject, content,
                ]
            ],
            Featured Listing => [
                if analysis does not exist, show generating
                else, show targets and target achieved
            ]

        ]
    ],
    Subscriptions => [
        List the subscriptions in table with filter on active. 
        Allow users to change filter to inactive
        For each subscription, show number of seats, plan, start date, expiry date, button to renew, invoice
    ],
    
]